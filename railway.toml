[build]
builder = "nixpacks"
buildCommand = "pip install -r requirements.txt && pip install ."

[deploy]
startCommand = "uvicorn aaplpredict.api.main:app --host 0.0.0.0 --port $PORT"
healthcheckPath = "/health"
healthcheckTimeout = 30
restartPolicyType = "on_failure"
